export const sum = (first, second) => {
    return first + second
}

export const sumAll = (numbers, callback) => {
    let total = 0
    for (number of numbers) {
        total += number
    }

    callback(total)
}